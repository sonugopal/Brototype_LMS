/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "utfs.io",
      'via.placeholder.com',
      'cdn.pixabay.com',
      'i.postimg.cc'
    ],
  }
}

module.exports = nextConfig
