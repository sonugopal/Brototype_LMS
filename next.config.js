/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "utfs.io",
      'via.placeholder.com',
      'cdn.pixabay.com'
    ],
  }
}

module.exports = nextConfig
